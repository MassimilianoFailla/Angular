import { MySearch } from './MySearch';
export class MyData{

    nome: string;
    cognome: string;
    id: number;

    constructor(nome: string, cognome: string, id: number){
        this.nome = nome;
        this.cognome = cognome;
        this.id = id;
    }
    
}